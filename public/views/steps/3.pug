br
h1(class="title is-3")=locale.preference
br
form(method="post" action="/ilmo/3")
  each pref in preferences
    div(class="box")
      br
      if pref.id == chosenPreference
        input(type="radio" id=pref.id name='preference' value=pref checked)
      else
        input(type="radio" id=pref.id name='preference' value=pref)
      label(for=pref.id class="radio-label")
        p(class="title is-5")=pref.name
        p=pref.description
        br
        b=(pref.price / 100) + '€'
        br
  br
  input(type="submit" value=locale.next class="button")